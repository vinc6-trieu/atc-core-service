export enum ENewsEventsStatus {
  Draft = 'draft',
  Private = 'private_noindex',
  Redirect = 'redirect_noindex',
  PublicNoIndex = 'public',
  PublicIndex = 'public_index',
  Deleted = 'deleted',
}

export enum ENewsEventType {
  News = 'news',
  Events = 'events',
}
