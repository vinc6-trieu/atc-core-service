export enum EPageTypeStatus {
  Active = 'active',
  Deactive = 'deactive',
}

export enum ERenderType {
  Static = 'static',
  Server = 'server',
  Client = 'client',
}

export enum EOrderBy {
  ASC = 'asc',
  DESC = 'desc',
}
